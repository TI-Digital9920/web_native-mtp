# Jika membuat file insert, delete, update (query sql) tambahkan perintah di bawah ini pada awalan perintah php di setiap file anda :
<!-- if (!defined('APP_SECURE')) {
    die('Access denied.');
} -->